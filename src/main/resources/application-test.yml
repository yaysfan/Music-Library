# 'test' 프로필에서만 적용될 설정

spring:
  cloud:
    aws:
      secretsmanager:
        enabled: false
      stack:
        auto: false
      region:
        auto: false

config:
  file:
    base-path: "dummy" # YtDlpAdapter에서 사용하는 값
  yt-dlp:
    path: "dummy"
    ffmpeg-directory: "dummy"
    cookies-file: "dummy"